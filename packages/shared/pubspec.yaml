name: shared
version: 0.1.0
publish_to: 'none'
description: Shared data layer (Drift schema, CSV, security) for Desktop and Mobile apps.
environment:
  sdk: ">=3.3.0 <4.0.0"
dependencies:
  drift: ^2.20.0
  path: ^1.9.0
  csv: ^5.1.1
  crypto: ^3.0.3
  collection: ^1.18.0
  meta: ^1.12.0
dev_dependencies:
  drift_dev: ^2.20.0
  build_runner: ^2.4.12

flutter:
  # This package is shared across platforms, sqlite3 is excluded in main app's web build
